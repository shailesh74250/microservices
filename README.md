# Serach Indexing Service
- Responsibilities:
    - Enables searching through chat history
    - Uses Elasticsearch for fast indexing and retrieval
    - Supports search filters (date, sender, attachments, etc.)
    - Tech Stack: Elasticsearch, PostgreSQL